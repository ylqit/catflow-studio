# 长短片统一生产实施记录

日期：2026-09-19。状态：故事导入完成，后续制作交由用户触发。代码主体已实现，但不宣称完整工程与内容验收通过。

用户最新要求：不再执行回归、专项测试或真实链路验证，只导入故事。已停止后续生成操作；当前视频生成任务为 **0**，AI 审片为 **0**。本机后台继续处理已经提交的任务，不自动创建后续制作任务。

## 基线与备份

- 改造前 Vue：32 文件、241 项通过。
- 改造前后端基线：171 通过、5 失败（达到 maxfail=5 后停止，不代表其余测试均通过）。已见问题为既有表清单遗漏和编辑边界/参考准备测试与现行规则不一致。
- 原库：catflow_studio_recovery_20260914，未改名、未覆盖。
- 完整备份：`var/backups/recovery-full-20260919T113003Z.zip`，SHA256 `c764e66434c737baf6c42bd441e3fbc1343275e465dfd13135d42e58d474418b`，479205343 字节。39 表、16 项目、6 系列、72 任务、126 资产、7412 文件；CRC、哈希和缺失引用检查通过。
- 隔离恢复校验库：catflow_restore_check_20260919。恢复外键检查通过，0035→0036 迁移通过；该库没有启动 Worker。详情 `var/work/narrative-production-20260919/restore-check.json`。
- 原有提交未知任务 `7fb44927-321a-49aa-ae4e-f8bbccbc4db3` 保留；没有重新提交。
- 业务库迁移：已升级至 `0036_narrative_production`。38 个历史业务表逐表行数与内容摘要一致；新表为空、外键约束全部有效，旧 72 个任务保留。详情 `var/work/narrative-production-20260919/migration.json`。

## 工程进度

已建立新叙事/分镜契约、计划版本、分组、依赖、实际参考编译、生产任务、选片和真实结束帧、EDL v3 组装，接入工作区。工程验收尚在进行。

用户随后明确要求跳过后续回归和专项测试，已停止追加运行。已取得的结果：核心 52 项通过；独立的 Worker 恢复、生产计划并发及实际 FFmpeg 360/1080/1440 帧渲染共 65 项通过；Vue 33 文件 244 项通过；类型检查及生产构建通过。完整后端一轮为 540 通过、31 失败：包含旧编辑用例未准备实际参考、旧最小时长断言，以及共享数据库排队残留对后续 Worker 用例的干扰。独立恢复测试通过，但不能据此声称完整回归已经全绿。

新增 HTTP→Worker Mock 请求用例中，视频文本与道具入参链路已通过；纯道具首帧用例暴露了旧编译器强制两角色走位的问题，已改为只校验入画角色。规划提示词旧断言和整数秒展示也已修正。遵照用户要求，最后这些修改没有再次运行专项测试。交付中区分已执行结果与未复测修改。

## 清理清单

| 处理 | 内容 | 依据 |
|---|---|---|
| 合并 | 时长能力入口 | API runtime/bootstrap 与前端共用能力状态，区分作品、镜头、模型时间 |
| 合并 | 单元角色/场景/道具编译 | production_compiler 统一职责与顺序，不按字符串去重删除指令 |
| 修正 | 表清单测试 | 包含已有角色选择表与新增生产表；基线迁移测试只期待其当年表 |
| 保留 | 整片、逐镜、局部编辑 | 有实际 API、页面和历史恢复职责 |
| 保留 | EDL v1/v2/v3、旧导演与任务 | 已存在持久化数据和冻结任务，不重新解释 |
| 保留 | 白猫历史预览、Canon、MP4 | 本轮没有证明无引用，不删除 |
| 保留 | 旧迁移档案 | 追溯原业务库和历史导入，不重新开放日常入口 |
| 删除 | 暂无确认无调用实现 | 不为制造清理数量删除仍有职责的代码 |

## 行为变化

新计划写入、激活、选片、组装与任务创建使用预期版本/哈希。未知或未完成单元任务阻止重复付费；原来的任务恢复协议保留。新路由的结构错误返回 422，来源变化返回 409。旧 Worker 不能提交新生产任务。非首段音频增加经校验的生产单元来源；显式静音不会偷偷保留候选音轨。局部替换后清除旧组装的事件证据，不自动认为内容继续通过。

## 两条样片

已开始约定范围内的付费调用。故事 2、导演 2、视频 4、图片最多 3、独立 AI 审片 0、默认重抽 0。提交前清单与操作日志保存在 `var/work/narrative-production-20260919/actual-request-manifest.json`、`run-state.json`，最终随交付复制至导出目录；用量按回执记录，未知价格不写成零。

项目：15 秒 `402b2c75-75c5-459f-8c4f-cbec1bdfa775`；45 秒 `6ae255ad-5ecf-44b8-86c2-071e19ca76fd`。两者显式绑定 V4 白猫。故事已采用，3 张新增资产完成。软垫参考的材质偏摄影感，纸箱盖未完全贴壁，作为外观结构参考保留；视频请求明确采用画风板的数字插画材质，不新增图片重抽。

最新交接时：故事 2 次完成并采用；图片 3 次完成；导演 2 次已提交（15 秒已返回，45 秒当时仍处理中）；视频 0 次。实际状态保存在任务记录中，处理中的任务可从页面查看，不需要重复提交。

故事入口：[15 秒版](http://127.0.0.1:8877/projects/402b2c75-75c5-459f-8c4f-cbec1bdfa775/planner)、[45 秒版](http://127.0.0.1:8877/projects/6ae255ad-5ecf-44b8-86c2-071e19ca76fd/planner)。两者是独立作品，不将 45 秒拆成多个系列单集。

用户可在“分镜画布”查看已有导演候选，确认后再进入“生产计划与选片”绑定道具和分组。45 秒作品使用三个合法生成单元；不要使用旧整片一次生成入口。本轮没有 MP4 交付、真实帧数／音轨检查或内容通过结论，这些环节均交由用户自行触发。
